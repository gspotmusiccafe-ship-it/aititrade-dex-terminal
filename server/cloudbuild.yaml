steps:
  - name: 'node:20'
    entrypoint: 'npm'
    args: ['install']
  - name: 'node:20'
    entrypoint: 'npm'
    args: ['run', 'build']
  # We pinned the version and pointed it to the folder
  - name: 'gcr.io/cloud-builders/firebase:3.12.0'
    args: ['deploy', '--project', 'aititrade-exchange-pro', '--only', 'hosting', '--config', 'server/firebase.json']

options:
  logging: CLOUD_LOGGING_ONLY
